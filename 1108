programa {
  funcao inicio() {
    //elaborar um programa tipo calculadora
    //receber dois numeros reais 
    //receber o operador matematico: = - * /
    //efetuar a operaçao escolhida com os numeros digitados.
    //ex: num1, num2, op: '+'
    //num1 + num2 
    //ATENÇAO: caso o operador seja / e o num2 seja zero
    //o aplicativo devera avisar o usuario que ocorrera uma divisao por zero!
    //operadores: -,*,/

    real num1, num2, resultado
    caracter operador
    escreva("Digite os numeros:")
    leia(num1)
    leia(num2)
    escreva("DIGITE A OPERAÇAO DESEJADA('+' '-' '*' '/')")
    leia(operador)
  se(operador == '+'){
    resultado = num1 + num2
    escreva("Resultado: ", resultado)
  }senao se(operador == '-'){
    resultado = num1 - num2
    escreva("Resultado: ", resultado)
  }senao se(operador == '*'){
    resultado = num1 * num2
    escreva("Resultado: ", resultado)
    }senao se(operador == '/'){
      resultado = num1 / num2
      escreva("Resultado: ", resultado)
    
